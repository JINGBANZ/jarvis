/// Namespace for predefined text sent to AI models.
///
/// The files in this directory are the single audit surface. Keep transport payloads, tool schemas,
/// and dynamic user/session content with their owning subsystems. Every app-owned instruction,
/// description, wrapper, and observation those payloads send to a model belongs in the matching
/// domain extension here.
public enum JarvisPrompts {}
