# Local review tooling — never part of the wiki (markdown is the source of truth)
wiki/*.html
wiki/build_html.py

# Swift build artifacts
.build/
*.app
*.xcodeproj
.DS_Store
secrets.env
.env
.env.*
!.env.example
*.key
*.p8
*.p12
*.pem
*.pfx
auth.json

# Dev-mode activity/debug logs (screen-derived content — never commit)
.jarvis/

# Local-only upstream license copies written by scripts/build-aec.sh (authoritative texts are in THIRD_PARTY_NOTICES.md)
Sources/CJarvisAEC/third_party/
