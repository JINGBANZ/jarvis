# The pinned reusable workflow gates every opened issue before its agent job: only an author with
# admin, maintain, or write permission can proceed. Its runner default is `ubuntu-latest`.
name: Issue Worker

on:
  issues:
    types: [opened]

concurrency:
  group: issue-worker-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  work:
    uses: JINGBANZ/workflows/.github/workflows/issue-worker.yml@0576cdaa7c17a5125960b68918f7d1e9ab0e7619
    permissions:
      contents: write
      pull-requests: write
      issues: write
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
      BOT_PAT: ${{ secrets.BOT_PAT }}
