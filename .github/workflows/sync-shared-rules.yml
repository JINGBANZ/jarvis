# Requires Settings → Actions → General → "Allow GitHub Actions to create and approve pull requests".
name: Sync shared rules

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

permissions: {}

jobs:
  sync:
    uses: JINGBANZ/workflows/.github/workflows/sync-shared-rules.yml@main
    permissions:
      contents: write
      pull-requests: write
